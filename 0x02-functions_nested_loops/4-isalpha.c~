#include "main.h"
#include <stdlib.h>
#include <stdio.h>
#include <ctype.h>

/**
 * _islower - Start of Script
 * @d: comes from main
 * Description:runs main from main.h
 * Return: Always 0 (Success)
 */
int _isalpha(char d)
{
if (isaplha(d) == 0)
{
return (0);
}
else
return (1);
}
