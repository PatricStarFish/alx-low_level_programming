#include "main.h"
#include <stdlib.h>
#include <stdio.h>
#include <ctype.h>

/**
 * _abs - Start of Script
 * int: comes from main
 * Description:runs main from main.h
 * Return: Always 0 (Success)
 */
int _abs(int)
{
return (0);
}
