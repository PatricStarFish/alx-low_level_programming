#include<stdio.h>

/**
 * main - Entry point
 * Description:printf funt
 * Return: Always 0 (Success)
 */
int main(void)
{
  char a;
  int b;
  long int c;
  long long int d;
  float e;
  
  int printf("size of a :%lu byte(s)\n",sizeof(char));
return (0);
}
